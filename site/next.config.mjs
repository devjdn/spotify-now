/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.scdn.co', 'thisis-images.spotifycdn.com', 'image-cdn-ak.spotifycdn.com'],
    },
  };
  
export default nextConfig;
  